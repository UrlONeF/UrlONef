# Netflix Memories AR

Un'esperienza di Realtà Aumentata che sovrappone un video ("memories") a una foto stampata (es. in una cornice), ispirata allo stile Netflix.
Realizzata con MindAR.js e A-Frame.

## 🚀 Come Mettere Online (GitHub Pages)

Questo progetto è pronto per essere ospitato gratuitamente su GitHub Pages.

### Passo 1: Carica su GitHub
1. Crea un **nuovo repository** su GitHub (es. `ar-memories`).
2. Carica tutti i file di questa cartella nel repository.
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/IL_TUO_USER/ar-memories.git
   git push -u origin main
   ```

### Passo 2: Attiva Pages
1. Vai sulla pagina del tuo repository su GitHub.
2. Clicca su **Settings** (in alto a destra).
3. Nel menu a sinistra, clicca **Pages**.
4. Sotto "Build and deployment" > "Branch", seleziona **main** e cartella **/(root)**.
5. Clicca **Save**.

Aspetta qualche minuto. GitHub ti darà un link (es. `https://tuonome.github.io/ar-memories/`).
Quello è il link da mandare alla tua ragazza!

## 📱 App "Standalone" (Android/iOS)
Questo sito è una **PWA** (Progressive Web App).
Quando apri il link dal telefono:
1. Apri il browser (Chrome su Android è ideale, Safari su iOS).
2. Apri il menu e seleziona **"Aggiungi a schermata Home"**.
3. L'app apparirà tra le tue applicazioni e si aprirà a tutto schermo senza barre del browser.
